{
  "name": "elite-minds-attendance-portal",
  "version": "1.0.0",
  "description": "Elite Minds Attendance Portal - Smart Attendance. Intelligent Analytics. Seamless Academic Management.",
  "main": "backend/server.js",
  "scripts": {
    "install:all": "npm install --prefix backend --include=dev && npm install --prefix frontend --include=dev",
    "build": "npm run install:all && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
