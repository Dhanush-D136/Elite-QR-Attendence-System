# Workspace Behavioral Rules

## Git Workflow
- After completing work on any user request or feature, automatically stage (`git add .`), commit with a descriptive commit message (`git commit -m "..."`), and push to GitHub (`git push origin main`).
